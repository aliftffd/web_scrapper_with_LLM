[package]
name = "web_scrapper_project"
version = "0.1.0"
edition = "2024"

[dependencies]

reqwest = { version = "0.12", features = ["blocking"]}
scraper = "0.19.0"
tokio = {version = "1", features = ["full"]}
